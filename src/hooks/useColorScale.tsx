import { useState } from "react"

const useColorScale = () => {
    
    const [color , setColor] = useState();

    // Indica la barra de humedad
    const showScaleColor = () =>{

    }

    return {
        color
    }

}